<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yangyang.java.ai.langchain4j.doctor.mapper.DoctorChatMessageMapper">
    <!-- 查询两个医生的聊天历史（双向） -->
    <select id="selectChatHistory"
            resultType="com.yangyang.java.ai.langchain4j.doctor.entity.DoctorChatMessage">
        SELECT * FROM doctor_chat_message
        WHERE (sender_id = #{senderId} AND receiver_id = #{receiverId})
           OR (sender_id = #{receiverId} AND receiver_id = #{senderId})
        ORDER BY send_time ASC
    </select>

    <select id="selectUnreadCountBySender" resultType="java.util.Map" parameterType="java.lang.Long">
        SELECT sender_id, COUNT(*) AS unread_count
        FROM doctor_chat_message
        WHERE receiver_id = #{receiverId}
          AND is_read = 0
        GROUP BY sender_id
    </select>

    <!-- 标记消息为已读 -->
    <update id="markAsRead">
        UPDATE doctor_chat_message
        SET is_read = 1
        WHERE sender_id = #{senderId}
          AND receiver_id = #{receiverId}
          AND is_read = 0
    </update>
</mapper>